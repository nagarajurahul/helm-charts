gatekeeper:
  disableValidatingWebhook: false

  # Mutation webhook is not generally used in produciton, unless advanced cases are needed
  # disableMutation: false

  # Enforce (fail-closed)
  validatingWebhookFailurePolicy: Fail

  # Commenting as we are not using mutating webhook
  # mutatingWebhookFailurePolicy: Fail
