apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quotes-app-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "quotes-app-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quotes-app-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
