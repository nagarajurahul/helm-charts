apiVersion: v1
kind: Service
metadata:
  name: {{ include "goflights.fullname" . }}
  labels:
    {{- include "goflights.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goflights.selectorLabels" . | nindent 4 }}
