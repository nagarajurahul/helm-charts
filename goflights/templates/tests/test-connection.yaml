apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "goflights.fullname" . }}-test-connection"
  labels:
    {{- include "goflights.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "goflights.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
